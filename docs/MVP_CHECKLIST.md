# Omniposter MVP Checklist

Last updated: 2026-05-24

Allowed statuses:

- Complete
- Partial
- Missing
- Not Verified

Do not mark an item `Complete` without evidence from code, tests, endpoint behavior, command output, or manual verification.

## Functional MVP

| Requirement | Status | Evidence | Notes |
|---|---:|---|---|
| Upload or select character images | Partial | `POST /character-presets/{preset_id}/portrait`; `frontend/src/pages/CharacterLibraryPage.tsx`; `backend/app/tests/test_vertical_slice.py::test_character_preset_portrait_upload_updates_visual_asset_only`; `scripts/mvp_smoke_verify.sh` uploaded portraits for Host and Guest on 2026-05-14 | Backend storage, MIME validation, portrait serving, and Docker smoke portrait upload are verified. Browser visual/upload UX still needs manual or Playwright verification. |
| Upload or select background videos from presets | Partial | `backend/app/tests/test_vertical_slice.py::test_background_presets_are_loaded_from_bundled_media_dir`; `test_image_background_upload_and_preset_selection`; `test_background_upload_rejects_declared_type_mismatch`; `test_project_preview_settings_save_load_background_speakers_and_layout`; `scripts/mvp_smoke_verify.sh` selected background preset `MC Parkour` for project `25` on 2026-05-14 | Background preset selection is Docker-runtime verified through the API; upload hardening now rejects declared MIME/header mismatches and enforces configured size/quota/duration gates. Manual browser UI verification still needed. |
| Write a two-speaker or multi-speaker dialogue script | Partial | `GET /script-generation/formats`; `POST /script-generation/generate`; `POST /projects/{project_id}/script/generate`; `frontend/src/components/script-generation/FormatBrowser.tsx`; `frontend/src/pages/ProjectEditorPage.tsx`; `frontend/src/components/command-room/CommandRoom.tsx`; `python3 -m pytest backend/app/tests/test_script_generation.py -q` passed with `39 passed` on 2026-05-24; `npm test -- --watchAll=false` passed with `7 passed` on 2026-05-21; `npm run build` passed on 2026-05-21 | Format-aware local-first generation exists and the backend script-generation regression suite is green. Debate fallback now has topic/sides normalization and hard validator coverage; manual browser verification still needed. |
| Parse script lines into speaker segments | Partial | `backend/app/services/scripts.py`; `backend/app/services/script_generation/service.py::generated_script_to_dialogue_lines`; `backend/app/tests/test_script_generation.py`; targeted `backend/app/tests/test_vertical_slice.py`; `python3 -m pytest backend/app/tests/test_script_generation.py` passed with `24 passed` on 2026-05-19; targeted vertical slice passed with `3 passed` | Manual `<Speaker> dialogue` parsing remains, and generated scripts are converted into render-compatible speaker lines with caption, section, and generated `line_id` metadata; manual UI verification still needed. |
| Map each speaker to a character image | Partial | `backend/app/tests/test_vertical_slice.py::test_project_preview_settings_save_load_background_speakers_and_layout`; `test_voice_profile_summary_exposes_associated_character_image`; `test_character_images_are_not_loaded_from_background_preset_directories`; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed on 2026-05-08 | Project preview settings and voice/profile summaries expose character portrait URLs from character storage only; manual UI verification still needed. |
| Map each speaker to a voice profile | Partial | `backend/app/tests/test_vertical_slice.py::test_generation_job_snapshots_selected_voice_profiles`; `test_generation_job_snapshots_preview_settings`; `test_saved_calibration_recipe_is_snapshotted_for_video_render`; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed on 2026-05-08 | Generation jobs snapshot selected speaker voice profile/provider payloads, saved calibration recipes, portrait metadata, and preview settings; real UI/runtime verification still needed. |
| Generate TTS audio per line or segment | Partial | `backend/app/tests/test_vertical_slice.py::test_local_speech_service_prefers_generation_voice_manifest`; `test_render_segment_metadata_exposes_safe_artifact_url`; `test_xtts_provider_uses_stewie_selected_recipe_exactly`; `test_xtts_render_job_synthesizes_exact_script_segments_to_persisted_wavs`; `test_tts_orchestrator_does_not_use_xtts_voice_cache_for_render_segments`; Docker API `XTTSProvider().synthesize_line(...)` wrote `/data/uploads/voice_models/stewie_griffin/previews/render_smoke/stewie_recipe_smoke_container.wav` with `provider_used='xtts'` and 6 reference WAVs; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed on 2026-05-06 | TTS layer uses persisted per-speaker voice manifest, processed reference WAV metadata, calibration recipe controls, optional XTTS/RVC provider adapters, and exposes per-segment render WAV metadata. Automated tests verify XTTS render jobs synthesize exact parsed script text to per-job segment WAVs and bypass shared XTTS cache; real full video-job XTTS runtime verification is still needed. |
| Assemble audio, background, captions/text overlays, and active speaker portraits into video | Partial | `backend/app/tests/test_vertical_slice.py::test_render_preview_final_mp4_uses_persisted_segment_wavs`; `test_xtts_render_job_synthesizes_exact_script_segments_to_persisted_wavs`; `test_ffmpeg_render_cache_reuses_tts_and_invalidates_changed_inputs`; `scripts/mvp_smoke_verify.sh` generated job `73` with ffprobe duration `9.166667`, `1` video stream, and `1` audio stream on 2026-05-14 | Docker smoke proves the two-speaker render produces a playable MP4 with audio/video streams from fallback TTS segment WAVs. Visual overlay/caption quality still needs browser/manual media review. |
| Preview generated output | Partial | `frontend/src/components/production/ProductionPreviewFrame.tsx`; `frontend/src/pages/ProjectEditorPage.tsx`; `frontend/e2e/project-editor-render.spec.ts`; `backend/app/tests/test_vertical_slice.py::test_project_preview_settings_save_load_background_speakers_and_layout`; `npm test -- --watchAll=false` passed with `9 passed`; `npm run build` passed on 2026-05-21; `npm run test:e2e` passed with `4 passed` on 2026-05-21 | Production Lab now centers a 9:16 preview frame using persisted scene, character image, caption style, and layout settings with missing-state overlays; authenticated Playwright coverage verifies distinct preview frame accessible labels and preview-setting updates with mocked same-origin API responses. Manual browser verification against actual generated media still needed. |
| Export generated video file | Complete | `backend/app/tests/test_vertical_slice.py::test_generated_media_lists_user_outputs_and_downloads`; `test_asset_content_reports_missing_storage_file`; `scripts/mvp_smoke_verify.sh` downloaded `/assets/73/content` and validated it with ffprobe on 2026-05-14 | Browser-facing asset/content URL returns the MP4 with download headers when present, missing storage files return useful 404 errors, and Docker smoke verified the downloaded file exists, has duration > 0, and has audio/video streams. |
| View job status, errors, and generation logs from UI | Partial | `frontend/src/components/RenderDiagnosticsPanel.tsx`; `frontend/src/components/command-room/CommandRoom.tsx`; `frontend/src/pages/ProjectEditorPage.tsx`; `frontend/src/pages/GeneratedMediaPage.tsx`; `backend/app/tests/test_vertical_slice.py::test_generation_job_artifacts_summary_exposes_debug_urls`; `npm test -- --watchAll=false` passed with `7 passed`; `npm run build` passed on 2026-05-19 | Shared render diagnostics UI still exposes status/progress/errors, voice/provider entries, segment WAVs, composite/final audio, final MP4, render plan, cache report, timing profile, provider metadata, and cache stats, with advanced diagnostics collapsed by default. Manual browser UX verification still needed. |

## Technical MVP

| Requirement | Status | Evidence | Notes |
|---|---:|---|---|
| FastAPI backend with clear REST endpoints | Not Verified | Needs repository audit | List endpoints and verify health/generation routes. |
| React + Tailwind frontend with generation forms and preview panels | Partial | `frontend/src/components/command-room/CommandRoom.tsx`; `frontend/src/components/production/ProductionReadinessPanel.tsx`; `frontend/src/components/production/ProductionPreviewFrame.tsx`; `frontend/src/pages/ProjectEditorPage.tsx`; `frontend/src/components/production/ProductionReadinessPanel.test.jsx`; `npm test -- --watchAll=false` passed with `9 passed` on 2026-05-21; `npm run build` passed on 2026-05-21 | Command Room now acts as a lower-density dashboard with next-best-action/readiness summaries; Production Lab now follows Idea, Script, Cast & Voices, Scene, Preview, Render, and Release steps with explicit Missing/Ready/Warning/Failed/Verified readiness states. Manual browser verification still needed. |
| Celery worker for long-running generation jobs | Partial | `backend/app/tasks/generation.py`; `backend/app/tasks/voice_operations.py`; `backend/app/tasks/voice_preview.py`; `backend/app/celery_app.py`; `deploy/compose/docker-compose.yml`; `backend/app/tests/test_vertical_slice.py::test_generation_job_rejects_when_user_active_job_limit_is_reached`; `test_voice_operation_reference_upload_uses_staged_file_without_read_bytes`; `python3 -m pytest backend/app/tests/test_vertical_slice.py -q` passed on 2026-05-21; `docker compose -f deploy/compose/docker-compose.yml config --quiet` passed on 2026-05-21 | Generation jobs and memory-heavy Voice Lab operations are queued through Celery; staged Voice Lab uploads are processed from disk in worker jobs; generation queue caps, task time limits, and optional worker memory-child limits are now configurable. Manual Docker replay is still needed for repeated real reference upload/calibration OOM verification. |
| Local storage structure for uploaded assets, generated files, voice profiles, and presets | Partial | `backend/app/services/storage.py`; `backend/app/tests/test_vertical_slice.py::test_background_upload_rejects_declared_type_mismatch`; `test_asset_content_reports_missing_storage_file`; `python3 -m pytest backend/app/tests/test_vertical_slice.py -q` passed on 2026-05-21 | Background uploads now use header-based MIME validation plus configured size/quota/duration gates. Broader storage topology and production object storage strategy remain not fully verified. |
| TTS provider abstraction | Partial | `backend/app/tests/test_vertical_slice.py::test_tts_orchestrator_does_not_fallback_when_openvoice_selected_but_unavailable`; `test_tts_orchestrator_does_not_fallback_when_xtts_selected_but_unavailable`; `test_generation_worker_persists_xtts_provider_failure`; `test_tts_provider_capabilities_route_returns_registry_state`; `test_stewie_selected_recipe_validates_required_paths`; `test_xtts_provider_uses_stewie_selected_recipe_exactly`; Docker API/worker import checks passed with `XTTS_ENABLED=True`; Docker API selected-recipe validation found provider `xtts`, checkpoint path, 6 references, and golden preview; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed on 2026-05-06 | Provider selection honors explicit OpenVoice/XTTS/RVC fail-closed policy, records provider diagnostics, exposes optional XTTS/RVC capability state, validates the Stewie selected XTTS recipe before use, and does not silently use fallback TTS when XTTS is selected. |
| Reliable fallback TTS provider that works inside Docker | Complete | `scripts/mvp_smoke_verify.sh` passed on 2026-05-14 with segment providers `Host=espeak, Guest=espeak`; `/health/deep` reported `checks.tts_fallback.ok=true`; job `73` produced segment WAV artifacts and final MP4 `/assets/73/content` | Docker runtime fallback TTS is verified through the normal project/render/provider path, not a disconnected helper. |
| OpenVoice V2 integration as clone-capable provider when configured | Partial | `/health/deep` in Docker on 2026-05-14 reported OpenVoice `enabled=true`, `available=true`, repo `/opt/openvoice/repo`, checkpoints `/opt/openvoice/checkpoints_v2`, device `cpu`, warning `gpu_unavailable_using_cpu`; provider fail-closed tests exist in `backend/app/tests/test_vertical_slice.py` | Diagnostics verify configuration/availability reporting, but the smoke render used `espeak`; real OpenVoice segment synthesis and full render audio remain unverified. |
| Health checks for API, worker, storage, ffmpeg, TTS providers, and OpenVoice availability | Complete | `GET /health/deep`; `backend/app/tests/test_vertical_slice.py::test_deep_health_reports_runtime_checks`; Docker `/health/deep` on 2026-05-14 reported API, DB/migrations, Redis, Celery workers, storage, ffmpeg, ffprobe, fallback TTS, OpenVoice, and XTTS ok/available; RVC disabled made overall status `degraded` | `/health` remains simple; `/health/deep` provides structured runtime diagnostics without exposing secrets. |
| Regression tests for critical rendering and TTS behavior | Partial | `python3 -m pytest backend/app/tests/test_vertical_slice.py -q` passed on 2026-05-24; `python3 -m pytest backend/app/tests/test_script_generation.py -q` passed with `39 passed` on 2026-05-24; `npm test -- --watchAll=false` passed with `9 passed`; `npm run test:e2e` passed with `4 passed` | Coverage includes voice manifest propagation, provider fail-closed behavior, persisted segment WAV assembly, FFmpeg render planning/cache behavior, generated media ownership, upload hardening, generation queue caps, production navigation/deep-link redirect coverage, authenticated Project Editor Render browser coverage, Ollama repair/fallback diagnostics, compact prompt budget, debate-format topic grounding, staged Voice Lab upload memory protection, and frontend production readiness state coverage. |

## Product Modules

| Module | Status | Evidence | Notes |
|---|---:|---|---|
| Dashboard | Partial | `frontend/src/App.tsx`; `frontend/src/components/command-room/CommandRoom.tsx`; `frontend/src/components/production/ProductionReadinessPanel.tsx`; `frontend/src/components/command-room/commandRoom.css`; `npm test -- --watchAll=false` passed with `7 passed`; `npm run build` passed on 2026-05-19 | `/` now renders Command Room as a dashboard with current production summary, one next-best-action path, readiness summary, active productions, queue summaries, and collapsed system diagnostics. Manual visual verification still needed. |
| Script Studio | Partial | `GET /script-generation/formats`; `POST /script-generation/generate`; `PATCH /projects/{project_id}/script-generation-settings`; `frontend/src/components/script-generation/FormatBrowser.tsx`; `frontend/src/pages/ProjectEditorPage.tsx`; `backend/app/tests/test_script_generation.py`; `python3 -m pytest backend/app/tests/test_script_generation.py -q` passed with `39 passed` on 2026-05-24; `python3 -m compileall backend/app` passed on 2026-05-24; `npm test -- --watchAll=false` passed with `7 passed`; `npm run build` passed on 2026-05-21 | Project Editor can browse/select reusable formats and persist generation settings; backend script-generation regressions are green, including debate topic grounding. Manual browser verification still needed. |
| Character Library | Partial | `frontend/src/pages/CharacterLibraryPage.tsx`; `POST /character-presets/{preset_id}/portrait`; `backend/app/tests/test_vertical_slice.py::test_character_preset_portrait_upload_updates_visual_asset_only`; `npm run build` passed on 2026-05-14 | Standalone route exists, portraits can be uploaded/changed, and the UI distinguishes visual portraits from voice profiles. Browser interaction and project-specific selection from this page still need verification. |
| Voice Lab | Partial | `backend/app/tests/test_vertical_slice.py::test_reference_audio_upload_normalizes_audio_and_invalidates_embedding`; `test_voice_operation_reference_upload_uses_staged_file_without_read_bytes`; `test_character_reference_dataset_upload_analyze_and_attach_model`; `test_character_calibration_batch_scores_and_saves_recipe`; `test_prepare_voice_profile_persists_embedding_metadata`; `backend/app/tasks/voice_operations.py`; `GET /voice-lab/operations/{job_id}`; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed with `93 passed` on 2026-05-11 | Voice Lab persists voice/replication state and queues memory-heavy reference processing, dataset analysis, model attach, profile prepare, and calibration batch work to `voice_worker`; staged upload processing no longer materializes the full pending file in worker memory. Full manual runtime UX/OOM verification still needed. |
| Background Presets | Partial | `backend/app/tests/test_vertical_slice.py::test_background_presets_are_loaded_from_bundled_media_dir`; `test_image_background_upload_and_preset_selection`; `test_project_preview_settings_save_load_background_speakers_and_layout`; `python3 -m pytest backend/app/tests/test_vertical_slice.py` passed with `96 passed` on 2026-05-13 | Video and image preset discovery plus project preview persistence are covered; manual UI/runtime verification still needed. |
| Video Generator | Partial | `backend/app/tests/test_vertical_slice.py::test_generation_job_snapshots_preview_settings`; `test_generation_worker_uses_persisted_voice_manifest_after_binding_changes`; `test_ffmpeg_render_cache_reuses_tts_and_invalidates_changed_inputs`; `frontend/src/pages/ProjectEditorPage.tsx`; `frontend/src/components/production/ProductionPreviewFrame.tsx`; `frontend/e2e/project-editor-render.spec.ts`; `npm test -- --watchAll=false` passed with `9 passed`; `npm run build` passed on 2026-05-21; `npm run test:e2e` passed with `4 passed` on 2026-05-21 | Production Lab now separates Cast & Voices, Scene, Preview, and Render steps while preserving persisted preview settings and draft/final/debug render actions; authenticated Playwright coverage verifies Render cache warmth, "What Changed?", readiness badges, accessible preview labels, and preview-setting updates with mocked API responses. Full browser verification against a real render job still needed. |
| Job Monitor | Partial | `backend/app/tests/test_vertical_slice.py::test_generation_job_list_exposes_latest_completed_job`; `test_generation_job_artifacts_summary_exposes_debug_urls`; `GET /projects/{project_id}/generation-jobs/latest`; `frontend/src/components/RenderDiagnosticsPanel.tsx`; `npm test -- --watchAll=false` passed with `7 passed`; `npm run build` passed on 2026-05-19 | Command Room and Project Editor expose latest render status while advanced artifact/provider/cache diagnostics are retained behind a collapsed disclosure; manual UX verification still needed. |
| Generated Media Library | Partial | `GET /generated-media`; `frontend/src/pages/GeneratedMediaPage.tsx`; `backend/app/tests/test_vertical_slice.py::test_generated_media_lists_user_outputs_and_downloads`; `scripts/mvp_smoke_verify.sh` output URL `/assets/73/content` on 2026-05-14; `npm run build` passed | Standalone route lists generated outputs across accessible projects and links preview/download/project/logs through existing asset routes. Manual browser verification still needed. |
| Upload / Publishing Prep | Partial | `frontend/src/components/command-room/CommandRoom.tsx`; existing metadata/publish routes; `npm run build` passed on 2026-05-13 | Command Room release/channel panels show local, paused, and active gating around existing publish support; real platform publishing remains intentionally limited to existing backend support and needs manual verification. |
| System Health and Settings | Partial | `GET /health/deep`; `backend/app/tests/test_vertical_slice.py::test_deep_health_reports_runtime_checks`; Docker `/health/deep` verified on 2026-05-14; `frontend/src/components/command-room/CommandRoom.tsx`; `npm run build` passed on 2026-05-19 | Backend runtime diagnostics are verified. Command Room shows collapsed “System status / diagnostics” so health details no longer dominate the dashboard. |

## Context System

| Requirement | Status | Evidence | Notes |
|---|---:|---|---|
| Root `AGENTS.md` exists | Complete | `AGENTS.md` | Codex read-first and behavior rules generated. |
| `docs/AGENT_BRIEF.md` exists | Complete | `docs/AGENT_BRIEF.md` | Short always-read project memory summary added. |
| `docs/CONTEXT_INDEX.md` exists | Complete | `docs/CONTEXT_INDEX.md` | Defines when to read each full context doc. |
| `docs/TASK_ROUTING.md` exists | Complete | `docs/TASK_ROUTING.md` | Maps common task types to relevant docs and code areas. |
| `docs/REPO_MAP.md` exists | Complete | `docs/REPO_MAP.md` | Concise backend/frontend/runtime/storage/test map for targeted inspection. |
| `docs/PROJECT_MANUAL.md` exists | Complete | `docs/PROJECT_MANUAL.md` | Product intent and MVP scope generated. |
| `docs/CURRENT_STATUS.md` exists | Complete | `docs/CURRENT_STATUS.md` | Conservative status generated; app behavior still needs audit. |
| `docs/KNOWN_MISTAKES.md` exists | Complete | `docs/KNOWN_MISTAKES.md` | Regression memory generated. |
| `docs/ARCHITECTURE_DECISIONS.md` exists | Complete | `docs/ARCHITECTURE_DECISIONS.md` | ADRs generated. |
| `docs/MVP_CHECKLIST.md` exists | Complete | `docs/MVP_CHECKLIST.md` | Checklist generated with evidence rules. |
| `docs/CODEX_WORKFLOW.md` exists | Complete | `docs/CODEX_WORKFLOW.md` | Audit/implementation/verification workflow generated. |

## Next Verification Targets

1. Browser-verify `/generated-media`, `/characters`, Command Room diagnostics, and Project Editor diagnostics.
2. Runtime-verify OpenVoice synthesis through a real generation job when clone checkpoints/profile are selected.
3. Manually review the smoke-generated MP4 for active speaker portrait/caption alignment.
4. Add Playwright coverage for generated-media download and character portrait upload flows.
